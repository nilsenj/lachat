import { PipeThreadsPipe } from './pipe-threads.pipe';

describe('PipeThreadsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeThreadsPipe();
    expect(pipe).toBeTruthy();
  });
});
